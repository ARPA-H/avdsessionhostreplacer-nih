name: Input Test

on:
    workflow_dispatch:
      inputs:
        environment:
          type: choice
          description: Choose environment to destroy
          options: 
          - test
          - prod
        # message:
        #   required: true
        # use-emoji:
        #   type: boolean
        #   description: Include 🎉🤣 emojis
        # environment:
        #   type: environment
        # message:
        #   required: true
        # use-emoji:
        #   type: boolean
        #   description: Include 🎉🤣 emojis
        # environment:
        #   type: environment
        # print_tags:
        #   description: 'True to print to STDOUT'
        #   required: true
        #   type: boolean
        # tags:
        #   description: 'Test scenario tags'
        #   required: true
        #   type: string
        # environment:
        #   description: 'Environment to run tests against'
        #   type: environment
        #   required: true

jobs:
    print-tag:
       runs-on: ubuntu-latest
       #if:  ${{ inputs.print_tags }} 
       steps:
        - name: Print the input tag to STDOUT
          run: echo The logLevel is ${{ inputs.environment }}
          